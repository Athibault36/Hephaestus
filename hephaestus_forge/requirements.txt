# HephaestusForge CLI and agent runtime
typer>=0.12.0
pydantic>=2.0
pydantic-settings>=2.0
rich>=13.0
pyyaml>=6.0
httpx>=0.27.0
huggingface_hub>=0.23.0
numpy>=1.24

# Deploy services (vision, dcc, tts)
fastapi>=0.111.0
uvicorn>=0.30.0
sse-starlette>=2.0.0
starlette-context>=0.3.6

# Mission Control live bridge (agent -> dashboard over Socket.IO)
python-socketio>=5.11

# GPU dev / autonomous worker
tiktoken>=0.7.0
sentence-transformers>=3.0.0

# Optional: local LLM server
# llama-cpp-python[server]  # install separately with CUDA wheel on Linux

# Optional: cloud NIM / training
# torch transformers peft trl datasets accelerate
